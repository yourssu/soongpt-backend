# Rusaint Service 환경 변수 예시
실제 사용 시 .env 파일로 복사하고 값을 설정하세요
cp env.example .env

# ============================================================
# 필수 환경 변수 (WAS와 동일한 값 사용)
# ============================================================

# 내부 JWT 검증용 시크릿 (WAS의 rusaint.internal-jwt-secret과 동일한 문자열)
# 주의: http-client.private.env.json의 internalJwt(토큰 값)가 아니라, 시크릿 키를 넣어야 함
INTERNAL_JWT_SECRET=local-internal-jwt-secret-min-32-chars

# pseudonym 생성용 시크릿 (WAS의 rusaint.pseudonym-secret과 동일한 문자열)
# 발급이 아님. WAS와 같은 임의의 문자열(32자 이상 권장)로 맞추면 됨
PSEUDONYM_SECRET=local-pseudonym-secret-min-32-chars

# ============================================================
# 선택 환경 변수 (기본값이 있음)
# ============================================================

# 개발 모드 (기본값: false). true면 internal-jwt-placeholder 허용
DEBUG=true

# Rusaint 타임아웃 (기본값: 30초)
# RUSAINT_TIMEOUT=30

# CORS 허용 origin (기본값: http://localhost:8080)
# ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000

# ============================================================
# Redis 설정 (Phase 3에서 사용 예정)
# ============================================================

# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=your-redis-password
