# Rusaint Service 환경 변수 예시
# 실제 사용 시 .env 파일로 복사하고 값을 설정하세요

# 애플리케이션 설정
APP_NAME=Rusaint Service
APP_VERSION=0.1.0
DEBUG=true  # 프로덕션에서는 false로 설정

# 내부 JWT 인증 (필수 - WAS와 동일한 시크릿 사용)
INTERNAL_JWT_SECRET=your-secret-key-here
INTERNAL_JWT_ALGORITHM=HS256

# Rusaint 설정
RUSAINT_TIMEOUT=30  # 초 단위

# CORS 설정 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000

# Redis 설정 (Phase 3)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your-redis-password  # 필요시 주석 해제
