### ========================================
### Python rusaint_service 직접 호출 (참고)
###
### 변수: studentId, sToken 은 http-client.private.env.json 의 "local" 에서 가져옴.
### ========================================

### 학적/성적 이력 조회 (약 4-5초)
POST http://localhost:8001/api/usaint/snapshot/academic
Authorization: Bearer internal-jwt-placeholder
Content-Type: application/json

{
  "studentId": "{{studentId}}",
  "sToken": "{{sToken}}"
}

###

### 졸업사정표 조회 (약 5-6초)
POST http://localhost:8001/api/usaint/snapshot/graduation
Authorization: Bearer internal-jwt-placeholder
Content-Type: application/json

{
  "studentId": "{{studentId}}",
  "sToken": "{{sToken}}"
}
